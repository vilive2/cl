#include<stdio.h>
#define MAX_LEN 100

void lis(int a[MAX_LEN], int len, int i, int j)

void printlis(int a[MAX_LEN], int len) {

} 

int main() {

    int a[MAX_LEN];

    int len = 0;
    while(scanf("%d", &a[len]) != EOF) len++;

    printlis(a, len);

    return 0;
}